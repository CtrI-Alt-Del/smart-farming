version: '3'

services:
 database:
    image: 'bitnami/mysql:latest'
    container_name: 'smart-farming-database'
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_DATABASE_PASSWORD}
      MYSQL_PASSWORD: ${MYSQL_DATABASE_PASSWORD}
      MYSQL_USER: ${MYSQL_DATABASE_USER}
      MYSQL_DATABASE: ${MYSQL_DATABASE_NAME}
    networks:
      - app-network
    volumes:
      - smart_farming_data:/bitnami/mysql/data
    ports:
      - 3306:3306
    healthcheck:
      test: ["CMD", "mysqladmin" ,"ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5

 app:
    build: .
    container_name: 'smart-farming-app'
    ports:
      - 8000:8000
    develop:
      watch:
        - action: sync
          path: .
          target: /home/pn/smart-farming
          ignore:
            - node_modules
    depends_on:
      database:
        condition: service_healthy
    networks:
      - app-network

networks:
 app-network:
    driver: bridge

volumes:
 smart_farming_data:
    driver: local
